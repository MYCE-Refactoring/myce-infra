services:
  myce:
    container_name: "{{ service_name }}"
    image: "{{ image_name }}:latest"
    restart: on-failure:3
    ports:
      - "{{ app_port }}:{{ app_port }}"
    volumes:
      - "{{ app_dir }}/logs:/app/logs"